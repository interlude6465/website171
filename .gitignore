*.log
photos/
.admin_config.json
latest_state.json
latest_state.json.bak
banned_ips.txt
banned_devices.txt
banned_fingerprints.json
approved_devices.txt
index.backup.html

# Local-only — never push (contains deploy runbook + server creds, Nyx core, harness config)
CLAUDE.md
SERVER-ACCESS.md
.claude/
nyx/

# Raw VIC font source folder (weights we use are copied into apk_loot/fonts/)
VIC font/

# Local-only reference screenshots & helper scripts
comparison photos/
claude_profiles.bat
claude_profiles.dat
